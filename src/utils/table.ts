// Table container classes - 表格容器样式
export const tableContainerClasses
  = 'rounded-lg border overflow-auto';

// Table container with max height - 带最大高度的表格容器样式
export const tableContainerMaxHeightClasses
  = 'rounded-lg border overflow-y-auto max-h-96';

// Table header classes - 表格头部样式
export const tableHeaderClasses
  = 'bg-muted sticky top-0 z-10 border-border rounded-t-lg';

// Table head classes - 表头样式
export const tableHeadClasses
  = 'px-3 py-2 text-left text-xs font-semibold uppercase tracking-wide border-b border-border text-muted-foreground';

// Table body classes - 表格主体样式
export const tableBodyClasses = ''

// Table row classes - 表格行样式
export const tableRowClasses = ''

// Table cell classes - 表格单元格样式
export const tableCellClasses
  = 'px-3 py-2 align-top text-sm border-b border-border/70 text-foreground';
